# Fahrevo OS — Supabase connection (public client values, safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://csutesdytvepazfmwvnk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_pzh1CeyHX0OilgFVE7A4kg_NtisLjC7

# Server-only secret used to sign the OTP-session cookie (proves the 2FA flow
# was completed, and enforces the 12-hour absolute session cap). Required —
# the app will throw if this is missing. Generate a real random value with:
#   openssl rand -hex 32
# Set the real value in your local .env.local (gitignored) and in your
# hosting provider's environment variables (e.g. Vercel project settings).
# Never commit the real value.
AUTH_SESSION_SECRET=replace-with-a-random-64-char-hex-value
